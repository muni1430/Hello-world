pipeline{
   agent any
      parameters {
             string:(defaultValue:'factor',description:'must be Enter a name',name:'Factor')
             booleanParam(name:'Boolean', defaultValue:true, desription:'Boolean value')
             }
       script {
       if(){
        }
        else(){
        }
        }
        }
        
